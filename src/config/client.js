/**
 * Configuración de branding por cliente.
 * Modifica este archivo para personalizar el dashboard para cada cliente.
 */
export const CLIENT_CONFIG = {
  // Nombre mostrado en el header
  name: 'NutraClínics',

  // Emoji para el logo (se usa si logoUrl es null)
  logo: '🥗',

  // URL de imagen personalizada (reemplaza el emoji)
  // Puede ser relativa al public folder: '/logo-cliente.png'
  // O una URL externa: 'https://...'
  logoUrl: null,
};
